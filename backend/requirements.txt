fastapi==0.141.1
uvicorn==0.52.0
mangum
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
python-multipart==0.0.32
email-validator>=2.0.0

# Supabase
supabase==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0
postgrest==2.31.0
realtime==2.31.0
storage3==2.31.0

# Auth token verification (Supabase Auth issues the actual session tokens;
# python-jose/passlib/bcrypt are kept only if you later choose to issue your
# own tokens - they are not used by the current login flow)
python-jose==3.5.0
passlib==1.7.4
bcrypt==5.0.0

# PDF generation
reportlab==5.0.0
pillow==12.3.0

# Third-party integrations
cloudinary==1.45.0
brevo-python==5.0.1

# HTTP / networking (transitive deps pinned by supabase/httpx)
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
h2==4.4.0
anyio==4.14.2

# --- The following were in the original requirements.txt but are unused by
#     the application code (no SQLAlchemy models are wired into any route,
#     and there is no Alembic migration workflow actually run). Removed to
#     shrink the dependency surface & install time. Re-add if you build out
#     the direct-Postgres path for real:
#     alembic, SQLAlchemy, psycopg2-binary, greenlet, Mako
